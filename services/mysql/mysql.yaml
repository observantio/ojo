service:
  name: ojo-mysql
  instance_id: mysql-0001

collection:
  poll_interval_secs: 10

mysql:
  executable: mysql
  # host: 127.0.0.1
  # port: 3306
  # user: root
  # password: ""
  # database: ""

export:
  otlp:
    endpoint: "http://localhost:4355/v1/metrics"
    protocol: http/protobuf
    timeout_secs: 10
  batch:
    interval_secs: 5
    timeout_secs: 10

metrics:
  include: [system.mysql.]
  exclude: []

storage:
  archive_enabled: false
  archive_dir: services/mysql/data
  archive_max_file_bytes: 67108864
  archive_retain_files: 8
  archive_file_stem: mysql-snapshots
  archive_format: parquet
  archive_mode: trend
  archive_window_secs: 60
  archive_compression: zstd
