{
  "__inputs": [
    {
      "name": "DS_PROMETHEUS",
      "label": "Prometheus",
      "description": "",
      "type": "datasource",
      "pluginId": "prometheus",
      "pluginName": "Prometheus"
    }
  ],
  "__requires": [
    { "type": "grafana", "id": "grafana", "name": "Grafana", "version": "11.0.0" },
    { "type": "panel", "id": "timeseries", "name": "Time series", "version": "" },
    { "type": "panel", "id": "stat", "name": "Stat", "version": "" },
    { "type": "datasource", "id": "prometheus", "name": "Prometheus", "version": "1.0.0" }
  ],
  "editable": true,
  "id": null,
  "uid": "ojo-postgres",
  "title": "Ojo Postgres",
  "schemaVersion": 39,
  "version": 3,
  "refresh": "10s",
  "tags": ["ojo", "postgres"],
  "time": { "from": "now-1h", "to": "now" },
  "templating": {
    "list": [
      {
        "name": "service",
        "type": "query",
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "query": "label_values(system_postgres_source_available, service_name)",
        "includeAll": true,
        "multi": true,
        "refresh": 1,
        "current": { "selected": true, "text": "All", "value": "$__all" }
      }
    ]
  },
  "panels": [
    {
      "id": 1,
      "type": "stat",
      "title": "Source Available",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [{ "refId": "A", "expr": "max(system_postgres_source_available{service_name=~\"$service\"})" }],
      "gridPos": { "h": 5, "w": 6, "x": 0, "y": 0 }
    },
    {
      "id": 2,
      "type": "stat",
      "title": "Postgres Up",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [{ "refId": "A", "expr": "max(system_postgres_up{service_name=~\"$service\"})" }],
      "gridPos": { "h": 5, "w": 6, "x": 6, "y": 0 }
    },
    {
      "id": 3,
      "type": "stat",
      "title": "Connections",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [{ "refId": "A", "expr": "max(system_postgres_connections{service_name=~\"$service\"})" }],
      "gridPos": { "h": 5, "w": 6, "x": 12, "y": 0 }
    },
    {
      "id": 4,
      "type": "stat",
      "title": "Deadlocks",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [{ "refId": "A", "expr": "max(system_postgres_deadlocks_total{service_name=~\"$service\"})" }],
      "gridPos": { "h": 5, "w": 6, "x": 18, "y": 0 }
    },
    {
      "id": 5,
      "type": "timeseries",
      "title": "Transactions / Second",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "refId": "A",
          "expr": "avg(system_postgres_transactions_committed_rate_per_second{service_name=~\"$service\"})",
          "legendFormat": "Committed"
        },
        {
          "refId": "B",
          "expr": "avg(system_postgres_transactions_rolled_back_rate_per_second{service_name=~\"$service\"})",
          "legendFormat": "Rolled Back"
        }
      ],
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 7, "w": 12, "x": 0, "y": 5 }
    },
    {
      "id": 6,
      "type": "timeseries",
      "title": "Buffer Blocks Read vs Hit",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "refId": "A",
          "expr": "sum(system_postgres_blocks_read_total{service_name=~\"$service\"})",
          "legendFormat": "Blocks Read"
        },
        {
          "refId": "B",
          "expr": "sum(system_postgres_blocks_hit_total{service_name=~\"$service\"})",
          "legendFormat": "Blocks Hit (cache)"
        }
      ],
      "gridPos": { "h": 7, "w": 12, "x": 12, "y": 5 }
    },
    {
      "id": 7,
      "type": "timeseries",
      "title": "Transaction Counters (Total)",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "refId": "A",
          "expr": "sum(system_postgres_transactions_committed_total{service_name=~\"$service\"})",
          "legendFormat": "Committed"
        },
        {
          "refId": "B",
          "expr": "sum(system_postgres_transactions_rolled_back_total{service_name=~\"$service\"})",
          "legendFormat": "Rolled Back"
        }
      ],
      "gridPos": { "h": 7, "w": 12, "x": 0, "y": 12 }
    },
    {
      "id": 8,
      "type": "timeseries",
      "title": "Deadlocks (Total)",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "refId": "A",
          "expr": "sum(system_postgres_deadlocks_total{service_name=~\"$service\"})",
          "legendFormat": "Deadlocks"
        }
      ],
      "gridPos": { "h": 7, "w": 12, "x": 12, "y": 12 }
    }
  ]
}
